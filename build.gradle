plugins {
    id "java"
    id "application"
}

group "duke"
version "0.5.0"

repositories {
    mavenCentral()
}

application {
    mainClassName = "duke.Main"
}

run {
    standardInput = System.in
}
