plugins {
    id 'java'
    id 'application'
    id 'checkstyle'
    // Defer use of Shadow and JUnit until a later stage
}

checkstyle {
    toolVersion = '8.23'
}

group 'seedu.duke'
version '0.1.0'

repositories {
    mavenCentral()
}

application {
    mainClassName = "Duke"
}
