plugins {
    id 'java'
    id 'application'
    id 'checkstyle'
    id 'com.github.johnrengelman.shadow' version '5.1.0' //shadow
    id 'org.openjfx.javafxplugin' version '0.0.7' //javafx
}

version '0.1.0'

//Shadow set-up
shadowJar {
    archiveBaseName = "duke"
    archiveVersion = "0.2"
    archiveClassifier = null
    archiveAppendix = null
}

repositories {
    mavenCentral()
}

//Javafx setup
javafx {
    version = "11.0.2"
    modules = [ 'javafx.controls', 'javafx.fxml' ]
}

//Gradle set-up
application {
    mainClassName = "ui.Launcher"
}

//Checkstyle setup
checkstyle {
    toolVersion = '8.23'
}

//JUnit set-up -------------
dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter:5.5.0'
}

test {
    useJUnitPlatform()
}
//--------------------------