import java.io.*;

public class Storage {
    File file;
    public Storage(String filepath) {
        this.file = new File(filepath);
    }


    /**
     * Process each line of the txt file to be understood by the constructor
     * @param line
     * @return processed line
     */
    private String txtProcessor(String line) {

    }
}