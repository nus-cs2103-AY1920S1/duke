/**
 * Class to represent the different unicode characters.
 */
public class Unicode {
    public static String SAD_FACE = new String(Character.toChars(0x2639));
    public static String CROSS = new String(Character.toChars(0x2715));
    public static String TICK = new String(Character.toChars(0x2713));
}
