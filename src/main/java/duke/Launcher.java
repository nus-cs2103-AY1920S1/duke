package duke;

import javafx.application.Application;

/**
 * This class is a workaround for classpath issues.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
