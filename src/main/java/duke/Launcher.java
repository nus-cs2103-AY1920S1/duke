package duke;

import javafx.application.Application;

/**
 * Launcher to workaround runtime issues
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Duke.class, args);
    }
}