package duke.gui;

import javafx.application.Application;

/**
 * Launcher for Duke, necessary to avoid classpath issues.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
